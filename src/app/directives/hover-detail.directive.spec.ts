import { HoverDetailDirective } from "./hover-detail.directive";


describe('HoverDetailDirective', () => {
  it('should create an instance', () => {
    //const directive = new HoverDetailDirective();
   //expect(directive).toBeTruthy();
  });
});
