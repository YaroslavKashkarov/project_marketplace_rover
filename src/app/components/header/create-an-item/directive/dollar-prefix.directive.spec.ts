import { DollarPrefixDirective } from './dollar-prefix.directive';

describe('DollarPrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new DollarPrefixDirective();
    expect(directive).toBeTruthy();
  });
});
